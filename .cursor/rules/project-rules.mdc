# AI 할일 관리 웹 서비스 - 프로젝트 개발 규칙

## 📋 프로젝트 개요

Next.js 15 App Router 기반의 AI 기능이 통합된 할일 관리 웹 애플리케이션입니다.
사용자 경험을 최우선으로 하며, 모든 상태(로딩/빈 상태/오류)를 직관적으로 표시하고 철저한 오류 처리를 제공합니다.

---

## 🛠 기술 스택

### Core Framework
- **Next.js 15** (App Router 필수)
- **TypeScript** (strict mode 권장)
- **React Server Components** (기본 렌더링 방식)

### Styling & UI
- **Tailwind CSS** (스타일링)
- **Shadcn/ui** (UI 컴포넌트 라이브러리)
- **Mobile-First** 반응형 디자인

### Backend & AI
- **Supabase** (인증, 데이터베이스, 실시간 기능)
- **AI SDK** (AI 모델 연동)
- **Server Actions** (데이터 변경 작업)

### Code Quality
- **ESLint** (Next.js/TypeScript 규칙 기반)
- **TypeScript Strict Mode**

---

## 📐 코드 스타일 가이드

### 1. 컴포넌트 작성 규칙

#### 함수형 컴포넌트 사용
```typescript
// ✅ 좋은 예: 화살표 함수로 작성
export const TodoItem = ({ todo }: TodoItemProps) => {
  return <div>{todo.title}</div>;
};

// ❌ 나쁜 예: function 키워드 사용
export function TodoItem({ todo }: TodoItemProps) {
  return <div>{todo.title}</div>;
}
```

#### 파일명 규칙
- **컴포넌트**: 파스칼 케이스 (예: `TodoItem.tsx`, `UserProfile.tsx`)
- **유틸리티/Hook**: 케밥 케이스 (예: `use-todo.ts`, `format-date.ts`)
- **디렉토리**: 케밥 케이스 (예: `todo-list/`, `user-settings/`)

### 2. 한글 주석 필수

모든 함수와 컴포넌트는 **한글 주석**으로 문서화해야 합니다.

```typescript
/**
 * 할일 항목을 표시하는 컴포넌트
 * @param todo - 표시할 할일 객체
 * @param onToggle - 완료 상태 토글 핸들러
 * @returns 할일 항목 UI
 */
export const TodoItem = ({ todo, onToggle }: TodoItemProps) => {
  // 완료 상태에 따라 스타일 변경
  const itemClass = todo.completed ? "line-through opacity-50" : "";
  
  return (
    <div className={itemClass} onClick={() => onToggle(todo.id)}>
      {todo.title}
    </div>
  );
};
```

### 3. 화살표 함수 일관성

모든 함수는 화살표 함수로 작성합니다.

```typescript
// ✅ 좋은 예
export const fetchTodos = async (userId: string) => {
  const { data, error } = await supabase
    .from('todos')
    .select('*')
    .eq('user_id', userId);
  
  if (error) throw error;
  return data;
};

// ❌ 나쁜 예
export async function fetchTodos(userId: string) {
  // ...
}
```

### 4. TypeScript 타입 정의

```typescript
// ✅ 모든 함수 매개변수와 반환값에 타입 명시
export const createTodo = async (
  title: string,
  userId: string
): Promise<Todo> => {
  // 구현
};

// ✅ Props 인터페이스 명시
interface TodoListProps {
  todos: Todo[];
  onUpdate: (id: string, updates: Partial<Todo>) => Promise<void>;
  isLoading?: boolean;
}
```

---

## 🎨 사용자 경험 (UX) 원칙

### 1. 모든 상태 UI 제공 필수

모든 데이터 로딩 영역에서 다음 3가지 상태를 제공해야 합니다:

#### 로딩 상태
```typescript
{isLoading && (
  <div className="flex items-center justify-center p-8">
    <Spinner className="h-8 w-8" />
    <span className="ml-2">할일을 불러오는 중...</span>
  </div>
)}
```

#### 빈 상태
```typescript
{!isLoading && todos.length === 0 && (
  <Empty
    icon={<CheckSquare className="h-12 w-12" />}
    title="등록된 할일이 없습니다"
    description="새로운 할일을 추가해보세요"
    action={<Button onClick={onAdd}>할일 추가</Button>}
  />
)}
```

#### 오류 상태
```typescript
{error && (
  <Alert variant="destructive">
    <AlertCircle className="h-4 w-4" />
    <AlertTitle>오류가 발생했습니다</AlertTitle>
    <AlertDescription>
      {error.message || "할일을 불러올 수 없습니다"}
    </AlertDescription>
  </Alert>
)}
```

### 2. 상호작용 지연 최소화

- **Optimistic Updates** 사용으로 즉각적인 피드백 제공
- **서버 컴포넌트** 우선 사용으로 초기 로딩 최적화
- **동적 임포트**로 필요한 컴포넌트만 로드

```typescript
// ✅ Optimistic Update 예시
const handleToggle = async (todoId: string) => {
  // 즉시 UI 업데이트
  setTodos(prev => 
    prev.map(todo => 
      todo.id === todoId 
        ? { ...todo, completed: !todo.completed }
        : todo
    )
  );
  
  try {
    // 서버 동기화
    await updateTodo(todoId);
  } catch (error) {
    // 실패 시 롤백
    setTodos(prev => 
      prev.map(todo => 
        todo.id === todoId 
          ? { ...todo, completed: !todo.completed }
          : todo
      )
    );
    toast.error("업데이트에 실패했습니다");
  }
};
```

---

## 🛡 오류 처리 규칙

### 1. 서버 컴포넌트/액션 오류 처리

```typescript
/**
 * 할일 목록을 가져오는 서버 액션
 */
export const getTodos = async (userId: string) => {
  try {
    const { data, error } = await supabase
      .from('todos')
      .select('*')
      .eq('user_id', userId);
    
    if (error) {
      // 개발 환경: 상세 로그
      if (process.env.NODE_ENV === 'development') {
        console.error('[getTodos Error]', error);
      }
      // 사용자에게 친절한 한글 메시지
      throw new Error('할일을 불러오는데 실패했습니다');
    }
    
    return data;
  } catch (error) {
    // 예상치 못한 오류 처리
    console.error('[getTodos Unexpected Error]', error);
    throw new Error('일시적인 오류가 발생했습니다. 잠시 후 다시 시도해주세요');
  }
};
```

### 2. 클라이언트 오류 처리

```typescript
'use client';

/**
 * 할일 생성 폼 컴포넌트
 */
export const CreateTodoForm = () => {
  const [error, setError] = useState<string | null>(null);
  
  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault();
    setError(null);
    
    try {
      await createTodo(title);
      toast.success("할일이 추가되었습니다");
    } catch (err) {
      // 개발 환경: 콘솔 출력
      if (process.env.NODE_ENV === 'development') {
        console.error('[CreateTodo Error]', err);
      }
      
      // 사용자에게 표시
      const message = err instanceof Error 
        ? err.message 
        : "할일 추가에 실패했습니다";
      setError(message);
    }
  };
  
  return (
    <form onSubmit={handleSubmit}>
      {error && <Alert variant="destructive">{error}</Alert>}
      {/* 폼 필드 */}
    </form>
  );
};
```

### 3. Error Boundary 사용

```typescript
// app/error.tsx
'use client';

/**
 * 전역 오류 경계 컴포넌트
 */
export default function Error({
  error,
  reset,
}: {
  error: Error & { digest?: string };
  reset: () => void;
}) {
  return (
    <div className="flex min-h-screen flex-col items-center justify-center">
      <Alert variant="destructive" className="max-w-md">
        <AlertCircle className="h-4 w-4" />
        <AlertTitle>오류가 발생했습니다</AlertTitle>
        <AlertDescription>
          {error.message || "예기치 않은 문제가 발생했습니다"}
        </AlertDescription>
      </Alert>
      <Button onClick={reset} className="mt-4">
        다시 시도
      </Button>
    </div>
  );
}
```

---

## 🏗 파일 구조 및 아키텍처

### 디렉토리 구조
```
app/
├── (auth)/              # 인증 관련 페이지 그룹
│   ├── login/
│   └── signup/
├── (dashboard)/         # 메인 대시보드 그룹
│   ├── todos/
│   └── settings/
├── api/                 # API 라우트
├── layout.tsx
└── page.tsx

components/
├── ui/                  # shadcn/ui 컴포넌트
├── todo/                # 할일 관련 컴포넌트
│   ├── TodoList.tsx
│   ├── TodoItem.tsx
│   └── CreateTodoForm.tsx
└── layout/              # 레이아웃 컴포넌트
    ├── Header.tsx
    └── Footer.tsx

lib/
├── supabase/           # Supabase 클라이언트 및 유틸
├── ai/                 # AI 관련 유틸
├── utils.ts            # 공통 유틸리티
└── validations.ts      # Zod 스키마

hooks/
├── use-todos.ts        # 할일 관련 훅
└── use-mobile.ts       # UI 관련 훅

types/
├── todo.ts             # 할일 타입 정의
└── user.ts             # 사용자 타입 정의
```

### 컴포넌트 분류

#### Server Components (기본)
- 데이터 페칭이 필요한 페이지
- 정적 레이아웃
- SEO가 중요한 컴포넌트

#### Client Components ('use client' 필요)
- 상호작용이 필요한 폼
- 상태 관리가 필요한 UI
- 브라우저 API 사용 (localStorage, etc.)

```typescript
// ✅ Server Component (기본)
export const TodoList = async () => {
  const todos = await getTodos();
  return <div>{/* 렌더링 */}</div>;
};

// ✅ Client Component
'use client';
export const TodoForm = () => {
  const [title, setTitle] = useState('');
  return <form>{/* 상호작용 */}</form>;
};
```

---

## ✅ ESLint 및 코드 품질

### ESLint 규칙 준수
- `next/core-web-vitals` 규칙 준수
- `@typescript-eslint` 규칙 준수
- 사용하지 않는 import/변수 제거
- `console.log` 프로덕션 빌드에서 제거

### 코드 리뷰 체크리스트
- [ ] 모든 함수에 한글 JSDoc 주석이 있는가?
- [ ] 화살표 함수로 일관되게 작성되었는가?
- [ ] 로딩/빈 상태/오류 상태 UI가 모두 구현되었는가?
- [ ] 오류 처리가 적절하게 되어 있는가?
- [ ] TypeScript 타입이 명시되어 있는가?
- [ ] ESLint 경고/오류가 없는가?
- [ ] 불필요한 'use client' 사용이 없는가?

---

## 🚀 성능 최적화 가이드

### 1. React Server Components 우선
```typescript
// ✅ 좋은 예: 서버 컴포넌트로 데이터 페칭
export const TodoPage = async () => {
  const todos = await getTodos();
  return <TodoList todos={todos} />;
};

// ❌ 나쁜 예: 클라이언트에서 불필요한 데이터 페칭
'use client';
export const TodoPage = () => {
  const [todos, setTodos] = useState([]);
  useEffect(() => {
    fetchTodos().then(setTodos);
  }, []);
  // ...
};
```

### 2. 동적 임포트
```typescript
// 무거운 컴포넌트는 동적 임포트
const AIAssistant = dynamic(() => import('@/components/AIAssistant'), {
  loading: () => <Spinner />,
  ssr: false,
});
```

### 3. 이미지 최적화
```typescript
import Image from 'next/image';

// ✅ Next.js Image 컴포넌트 사용
<Image
  src="/hero.png"
  alt="할일 관리"
  width={800}
  height={600}
  priority={false}
  loading="lazy"
/>
```

---

## 🔐 보안 가이드

### 1. 환경 변수 관리
```typescript
// ✅ 서버에서만 사용하는 시크릿
const apiKey = process.env.OPENAI_API_KEY; // 서버 컴포넌트/액션

// ✅ 클라이언트에 노출해도 되는 변수
const supabaseUrl = process.env.NEXT_PUBLIC_SUPABASE_URL;
```

### 2. 입력 검증
```typescript
import { z } from 'zod';

// Zod 스키마로 입력 검증
const todoSchema = z.object({
  title: z.string().min(1, "제목을 입력해주세요").max(200, "제목은 200자 이하여야 합니다"),
  description: z.string().optional(),
  dueDate: z.date().optional(),
});

export const createTodo = async (formData: unknown) => {
  // 입력 검증
  const validated = todoSchema.parse(formData);
  // DB 저장
};
```

### 3. 인증 확인
```typescript
/**
 * 인증된 사용자만 접근 가능한 서버 액션
 */
export const updateTodo = async (todoId: string, updates: Partial<Todo>) => {
  const user = await getCurrentUser();
  
  if (!user) {
    throw new Error("로그인이 필요합니다");
  }
  
  // 소유권 확인
  const todo = await getTodoById(todoId);
  if (todo.user_id !== user.id) {
    throw new Error("권한이 없습니다");
  }
  
  // 업데이트 수행
};
```

---

## 📝 커밋 메시지 규칙

```
feat: 새로운 기능 추가
fix: 버그 수정
docs: 문서 수정
style: 코드 포맷팅, 세미콜론 누락 등
refactor: 코드 리팩토링
test: 테스트 코드 추가
chore: 빌드 업무, 패키지 매니저 설정 등

예시:
feat: AI 할일 추천 기능 추가
fix: 할일 완료 토글 시 UI 미업데이트 문제 해결
```

---

## 🎯 개발 우선순위

1. **사용자 경험**: 로딩/빈 상태/오류 UI 필수
2. **오류 처리**: 모든 비동기 작업에 try-catch
3. **타입 안정성**: TypeScript strict mode 준수
4. **성능**: Server Components 우선, 불필요한 클라이언트 렌더링 최소화
5. **접근성**: 시맨틱 HTML, ARIA 레이블 사용
6. **보안**: 입력 검증, 인증/권한 확인

---

## 📚 추가 참고사항

- **AI 기능 통합 시**: 항상 로딩 상태와 오류 처리를 명확히 표시
- **Supabase 실시간 기능**: 구독 해제 로직 누락 방지 (`useEffect` cleanup)
- **다국어 지원 준비**: 모든 UI 텍스트를 상수로 분리 권장

---

이 규칙은 팀의 합의에 따라 지속적으로 업데이트됩니다.
마지막 업데이트: 2026-01-04
